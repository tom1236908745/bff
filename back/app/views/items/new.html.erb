<div class="main posts-new">
  <div class="container">
    <h1 class="form-heading">出品する</h1>
    <%= form_tag("/items/create") do %>
      <div class="form">
      <div class="form-body">
      <% @item.errors.messages.each do |message| %>
        <div class="form-error">
          <%= message %>
        </div>
      <% end %>
        <textarea name="content"></textarea>
        <input type="submit" value="出品">
      </div>
      </div>
    <% end %>
  </div>
</div>